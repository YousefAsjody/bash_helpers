# XCP-NG VDI Migration Script

This script automates the process of copying all VDIs attached to a XCP-ng VM
to another Storage Repository (SR).

## Usage
```bash
./vdi_copy.sh <VM_NAME> <TARGET_SR_UUID>
